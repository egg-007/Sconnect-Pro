<% if (typeof message !=='undefined' && message) { %>
    <div class="mb-6 rounded-xl border border-orange/20 bg-orange/10 px-4 py-3 text-sm font-semibold text-orange">
        <%= message %>
    </div>
    <% } %>